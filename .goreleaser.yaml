version: 2

before:
    hooks:
        - go mod tidy
builds:
    -   binary: unique
        env:
            - CGO_ENABLED=0
        goos:
            - linux
            - darwin
snapshot:
    version_template: "{{ incpatch .Version }}-next"
changelog:
    sort: asc
    filters:
        exclude:
            - '^docs:'
            - '^test:'

homebrew_casks:
    -   name: unique
        binaries:
            - unique
        description: "Outputs the unique lines of its input"
        homepage: "https://github.com/ro-tex/unique"
        directory: Casks
        url:
            template: "https://github.com/ro-tex/unique/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
            verified: "github.com/ro-tex/unique/"
        commit_author:
            name: goreleaserbot
            email: bot@goreleaser.com
        commit_msg_template: "Brew cask update for {{ .ProjectName }} version {{ .Tag }}"
        repository:
            owner: ro-tex
            name: homebrew-tap
            branch: main
            token: "{{ .Env.GITHUB_PERSONAL_AUTH_TOKEN }}"
            git:
                url: 'https://github.com/ro-tex/homebrew-tap.git'
                private_key: '/Users/inovakov/.ssh/id_ed25519'
                ssh_command: 'ssh -i /Users/inovakov/.ssh/id_ed25519 -o SomeOption=yes'
